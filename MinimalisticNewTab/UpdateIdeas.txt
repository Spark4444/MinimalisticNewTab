* Add search autocomplete results display
* add date display which scrolls at the bottom

2.
// Get the input element
let input = document.getElementById('myInput');

// Add an event listener for the input event
input.addEventListener('input', function() {
    // Get the value of the input field
    let query = input.value;

    // Define the URL for the API request
    let url = `http://suggestqueries.google.com/complete/search?client=chrome&q=${query}`;

    // Send a GET request to the API
    fetch(url)
        .then(response => response.json())
        .then(data => {
            // Log the autocomplete results to the console
            console.log(data[1]);
        })
        .catch(error => {
            // Log any errors to the console
            console.error('Error:', error);
        });
});